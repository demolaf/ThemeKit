# Example

SwiftUI and UIKit example apps demonstrating ThemeKit usage.

## Requirements

[xcodegen](https://github.com/yonaskolb/XcodeGen) — `brew install xcodegen`

## Setup

Run from this directory:

```sh
xcodegen generate
```

Then open `Example.xcodeproj`.
